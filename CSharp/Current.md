<!--
============================================================
ДЛЯ AI (Чат-помощник по C#)
============================================================

Привет! Это основной файл-навигатор моего конспекта по C# и .NET.

📌 СТРУКТУРА
- Папки: Junior/ Middle/ Senior/ – по уровням сложности.
- Файлы нумеруются: 010-Тема.md, 020-Тема.md и т.д. (шаг 10).
- Между темами можно вставлять 011-... или 011a-...
- Текущий файл (этот) – оглавление и рабочая область для новых тем.

👤 ОБО МНЕ (пользователь)
- У меня расстройство аутистического спектра (РАС).
- Мне нужна чёткая структура, без лишней информации и переключений.
- Я предпочитаю получать материал целиком (не разбивая на подпункты), если не прошу иначе.
- Я собираюсь использовать данный конспект и все файлы как HandBook, не залезая каждый раз в документацию, формируй материал так, чтобы у меня не было потребности лезть в документацию в большинстве сценариев разработки

🔍 КАК ТЫ ДОЛЖЕН РАБОТАТЬ СО МНОЙ
1. Всегда проверяй мои конспекты по официальной документации Microsoft.
2. Перед тем как давать новый материал – проверь, что я уже законспектировал, укажи на ошибки и оцени прогресс.
3. Новый материал давай сразу полностью (без деления на части), если я не говорю иначе.
4. Отвечай кратко и по делу, без воды.
5. Сообщай прогресс в процентах (сколько тем закрыто, сколько осталось).

📂 КАК МЫ РАБОТАЕМ С ЭТИМ ФАЙЛОМ
- Этот файл – оглавление и одновременно «песочница».
- Когда я изучаю новую тему, я пишу конспект прямо сюда (в соответствующий раздел).
- Позже, когда мне будет удобно, я перенесу содержимое в отдельный файл в папку уровня (Junior/Middle/Senior) и обновлю ссылку.
- Поэтому в этом файле могут быть как готовые темы (со ссылками), так и черновики (без ссылок).

✅ ТЕКУЩИЙ ПРОГРЕСС
- Junior: все темы закрыты (есть файлы).
- Middle: частично закрыты (есть файлы по Дженерикам, Делегатам, Асинхронности, Сериализации). Остальное – в плане.
- Senior: только Сборщик мусора. Остальное – в плане.

Планируемые темы (будут добавлены позже) перечислены в соответствующих разделах.

📌 КОГДА ТЫ НАЧИНАЕШЬ НОВЫЙ ДИАЛОГ
Просто прочитай этот файл – и ты будешь в курсе всей структуры, моих особенностей и нашего подхода.

📂 МОЙ РЕПОЗИТОРИЙ
- **URL:** https://github.com/kamnetanker/MyITBrain
- **Директория с конспектом:** `CSharp/`
- **Структура:** `Junior/`, `Middle/`, `Senior/`, файл `Current.md` (этот файл)
- **Доступ:** публичный, ты можешь читать любые файлы в любой момент

🔍 КАК ТЫ МОЖЕШЬ ИСПОЛЬЗОВАТЬ РЕПОЗИТОРИЙ
1. Если я прошу проверить конспект — ты открываешь нужный файл и сверяешь с документацией Microsoft.
2. Если я прошу дополнить тему — ты смотришь, что уже есть, и даёшь недостающее.
3. Если я прошу привести пример из реального проекта — ты можешь поискать подходящий репозиторий на GitHub (публичный) и показать его структуру или код.
4. Если я говорю «посмотри в моём репозитории» — ты знаешь, где искать.
5. Я обновляю конспекты (включая Current.md) локально в процессе работы, но в репозиторий на GitHub изменения попадают только в конце рабочей сессии (или после завершения блока тем). Поэтому актуальная версия на GitHub может отставать от моей локальной — если нужно свериться с последними правками, уточняй у меня.

============================================================
-->

# C# и .NET — дорожная карта (оглавление)

## Junior (база – обязательный минимум)

- [010 - Типы данных](./Junior/010-ТипыДанных.md)
- [020 - Математика](./Junior/020-Математика.md)
- [030 - Управляющие конструкции](./Junior/030-УправляющиеКонструкции.md)
- [040 - Структуры данных](./Junior/040-СтруктурыДанных.md)
- [050 - Строки](./Junior/050-Строки.md)
- [060 - Исключения](./Junior/060-Исключения.md)
- [070 - Функции](./Junior/070-Функции.md)
- [080 - Классы](./Junior/080-Классы.md)
- [090 - Интерфейсы](./Junior/090-Интерфейсы.md)
- [100 - Инкапсуляция](./Junior/100-Инкапсуляция.md)
- [110 - Наследование](./Junior/110-Наследование.md)
- [120 - Полиморфизм](./Junior/120-Полиморфизм.md)
- [130 - Потоки ввода-вывода](./Junior/130-ПотокиВвода-Вывода.md)
- [140 - Дата и время](./Junior/140-ДатаВремя.md)
- [150 - Замер времени](./Junior/150-ЗамерВремени.md)

---

## Middle (профессиональные инструменты)

- [010 - Дженерики](./Middle/010-Дженерики.md)
- [020 - Делегаты и события](./Middle/020-ДелегатыИСобытия.md)
- [030 - Асинхронность и многопоточность](./Middle/030-АсинхронностьИМногопоточность.md)
- [040 - Сериализация](./Middle/040-Сериализация.md)
- [050 - Рефлексия и атрибуты](./Middle/050-РефлексияАтрибуты.md)
- [060 - Принципы программирования](./Middle/060-ПринципыПрограммирования.md)

**Планируемые темы (будут добавлены позже):**

- Паттерны проектирования
- Архитектурные паттерны
- LINQ
- **Базы данных и SQL (основы)**
- **Entity Framework Core**
- ASP.NET Core Web API
- **Тестирование (xUnit, Moq)**
- **Логирование (Serilog)**
- **Кэширование (MemoryCache, Redis)**
- **Фоновые задачи (BackgroundService, Hangfire)**

---

## Senior (экспертиза и глубина)

- [010 - Сборщик мусора](./Senior/010-СборщикМусора.md)

**Планируемые темы (будут добавлены позже):**  

- Проекты (структура `.sln` / `.csproj`)
- Git и GitHub workflow
- NuGet
- Модель памяти .NET (Stack, Managed/Unmanaged Heap, барьеры)
- Продвинутая отладка и профилирование
- Безопасность в .NET (криптография, защита от атак)
- **Микросервисы (gRPC, Message Queues)**
- **Контейнеризация и оркестрация (Docker, Kubernetes — основы)**
- **Наблюдаемость (Prometheus, OpenTelemetry)**
- **Безопасность (JWT, OAuth2)**
- **Профилирование и оптимизация производительности**
- Blazor WASM
- SourceGenerators

---

## Текущие темы (ещё не распределены)

Эти темы уже есть в `Current.md` и ждут своего часа. Они будут добавлены в соответствующие разделы после того, как я создам для них файлы.

- Паттерны проектирования (`Middle/070-ПаттерныПроектирования.md`)
- Архитектурные паттерны (`Middle/080-АрхитектурныеПаттерны.md`)
- Проекты (структура `.sln` / `.csproj`) (`Senior/020-УправлениеПроектами.md`)
- Git и GitHub workflow (`Senior/030-GitWorkflow.md`)
- NuGet (`Senior/040-NuGet.md`) 
- LINQ (`Middle/090-LINQ.md`)
- ASP.NET Core Web API (`Middle/100-ASPNETCoreWebAPI.md`)
- Blazor WASM (`Senior/050-BlazorWASM.md`)
- Методология и процессы (XP и др.) (`Senior/060-Methodology.md`)

## Планируемые практические темы

### Создание приложения с поддержкой плагинов

**Цель:** научиться проектировать приложения так, чтобы третьи лица могли расширять их функциональность без изменения основного кода (десктоп, мобилка, игры).

**Подтемы:**
- Архитектура "Хост → Контракт → Плагин"
- Создание общего контракта (интерфейсы `IPlugin`, `ICommand` и т.п.)
- Динамическая загрузка плагинов через рефлексию
- Современная изоляция: `AssemblyLoadContext` и `AssemblyDependencyResolver`
- Интеграция с Dependency Injection (DI)
- Обнаружение плагинов через кастомные атрибуты
- Жизненный цикл плагина (загрузка → инициализация → активация → выгрузка)
- Ограничения мобильных платформ (iOS AOT, Android)
- Безопасность и песочница

---

**Практический пример:** создание калькулятора с командами, где каждая команда — отдельный плагин (пример из документации Microsoft).

### Разработка игры на Godot / Redot Engine (C#)

**Цель:** освоить создание игр на Godot (или его форке Redot) с использованием C# — от настройки среды до публикации на десктоп и мобильные платформы.

**Подтемы:**
- Установка Godot (.NET-версия) и настройка редактора под C#
- Создание первого скрипта на C# (вместо GDScript)[reference:0]
- Основы Godot API из C#: Node, SceneTree, сигналы, таймеры[reference:1]
- Работа с 2D/3D-физикой и анимациями через C#
- Интеграция сторонних .NET-библиотек (NuGet) в игровой проект
- Экспорт проекта на десктоп (Windows, Linux, macOS)[reference:2]
- Экспорт на Android и iOS (AOT-компиляция, особенности)[reference:3][reference:4]
- Создание игровых систем: состояния, компоненты, инвентарь, сохранения

**Практический пример:** создание простой 2D-игры (арканоид/платформер) с нуля на C#.

**Ресурсы:** Microsoft Godot C# Essentials[reference:5], официальная документация Godot C#[reference:6].

---

### Мобильное приложение-чат для локальной сети (P2P)

**Цель:** разработать кроссплатформенное мобильное приложение для обмена сообщениями напрямую между устройствами в одной локальной сети, без центрального сервера.

**Подтемы:**
- **Обнаружение соседей (Discovery):** mDNS / Zeroconf / Bonjour[reference:7] — автоматический поиск других устройств в локальной сети.
- **Peer-to-Peer соединение:** установка прямого TCP-соединения между найденными устройствами[reference:8].
- **Альтернативный подход:** WebRTC с DataChannel для P2P-связи (обход NAT, работа через интернет)[reference:9][reference:10].
- **Обмен сообщениями:** текстовые сообщения, уведомления о статусе (онлайн/офлайн), история чата.
- **Кроссплатформенность:** .NET MAUI / Avalonia UI / Uno Platform для единого кода под Android и iOS.
- **Разрешения:** сетевые разрешения на мобильных платформах (Android, iOS)[reference:11].
- **Безопасность:** шифрование сообщений (опционально).

**Практический пример:** консольный или мобильный чат-клиент, который автоматически находит соседей в Wi-Fi сети и позволяет обмениваться сообщениями.

**Ресурсы:** Zeroconf (NuGet)[reference:12], примеры P2P-чатов на GitHub[reference:13][reference:14].

### Веб-приложение "Менеджер задач" (Todo API) на ASP.NET Core

**Цель:** создать полноценный бэкенд для управления задачами с хранением в базе данных, авторизацией, логированием и интеграцией с внешним API. Это классический проект, который показывает, как работают вместе все ключевые Middle-технологии.

**Подтемы:**
- Проектирование моделей данных (Entity Framework Core, Code First, миграции)
- RESTful API на ASP.NET Core (контроллеры, маршрутизация, модель-биндинг, валидация)
- Dependency Injection (регистрация сервисов, работа с ILogger, IConfiguration)
- Работа с БД: PostgreSQL / SQLite / SQL Server через EF Core (запросы, LINQ, отношения)
- Аутентификация и авторизация (JWT, регистрация/вход пользователей)
- Кэширование (IMemoryCache, IDistributedCache + Redis)
- Логирование (Serilog / ILogger, structured logging, запись в файл или консоль)
- Фоновые задачи (IHostedService / BackgroundService) — например, очистка старых задач
- Интеграция с внешним API (например, получение курсов валют или погоды через HttpClient)
- Тестирование: Unit-тесты (xUnit + Moq), интеграционные тесты (TestContainers)
- Документирование API (Swagger / OpenAPI)

**Практический пример:** API для управления задачами с пользователями, категориями, приоритетами, сроками выполнения и уведомлениями.

**Ресурсы:** официальные туториалы Microsoft по ASP.NET Core, EF Core, JWT, xUnit, Serilog.

---

# Текущая работа

## Паттерны проектирования

В 1994 году вышла книга «Design Patterns: Elements of Reusable Object-Oriented Software» — её написали Эрих Гамма, Ричард Хелм, Ральф Джонсон и Джон Влиссидес. Четверо авторов, которых в народе прозвали «Бандой четырёх» (Gang of Four, GoF)

Они собрали 23 паттерна — типовых решений для часто встречающихся проблем в ООП-дизайне.

Что такое паттерн: Это не готовый код, который можно скопировать, а описание решения — схема, идея, подход. Это как рецепт: ингредиенты и последовательность действий известны, но каждый повар готовит по-своему.

### Критика и почему к паттернам нужно относиться с осторожностью
Главная проблема: Книгу GoF многие читают как сборник рецептов, а не как сборник идей. В результате разработчики начинают пихать паттерны везде, где только можно и где нельзя.

Основные претензии к GoF:
- Паттерны привязаны к C++ — книга писалась под возможности C++ 90-х. В современных языках многие проблемы решаются проще.
- «Подгоняем проблему под решение» — разработчики ищут, куда бы применить паттерн, вместо того чтобы искать простое решение.
- Спекулятивный дизайн — когда ты создаёшь гибкость, которая никому не нужна, «на всякий случай».
- Догматизм — паттерны начинают использовать как религиозные догмы, а не как инструменты.

Цитата из критики:

«Design Patterns — это корм для безмозглых программистов, неспособных к самостоятельному мышлению, которые будут производить код, столь же посредственный, как и паттерны, которые они используют»

Жёстко, но доля правды есть. Паттерны — это инструмент, а не цель.

### Сценарии применения паттернов

Необходимо сразу определиться, что решать несуществующие проблемы - путь в никуда, проблемы необходимо решать по мере их поступления.

1. Ты уже столкнулся с проблемой, которую решает паттерн - используй паттерн
2. Ты думаешь, что в будущем возникнет проблема - **НЕ используй паттерн**
3. Простое решение из 5 строк закроет задачу здесь и сейчас - **НЕ используй паттерн**
4. Код разрастается, спагеттифицируется, а паттерн реально причешет это безобразие - используй паттерн
5. "Так все делают" - **НЕ используй паттерн**

KISS и YAGNI — твои лучшие друзья. Паттерн, который усложняет код вместо того чтобы упрощать — антипаттерн в данном контексте.

### Классификация паттернов GoF

Всего GoF предложили 23 паттерна, разделённые на 3 группы:

- Порождающие - позволяют гибко создавать объекты. (`Singleton`, `Factory`, `Builder`, `Abstract Factory`, `Prototype`)
- Структурные - позволяют гибко управлять связями между объектами (`Adapter`, `Decorator`, `Facade`, `Proxy`, `Composite`, `Bridge`, `Flyweight`)
- Поведенческие - общение объектов друг с другом (`Observer`, `Strategy`, `Command`, `Template Method`, `Iterator`, `State`, `Visitor`, `Mediator`, `Memento`, `Chain of Responsibility`, `Interpreter`)

Мы разберём самые полезные паттерны, которые реально пригодятся в повседневной разработке. Остальные — либо устарели, либо используются в специфических задачах.

### Порождающие паттерны

1. Factory Method (Фабричный метод)

    Проблема категории Г: у тебя есть код, который должен создавать объекты, но заранее неизвестно, какой именно класс потребуется создать

    Решение: Создаётся метод, который возвращает объект нужного типа. Подклассы переопределяют этот метод и возвращают свои объекты, требуемого типа.

    ```c#
    // ❌ Плохо: жёсткая привязка к конкретному классу
    класс Отчёт {
        метод Создать() {
            документ = новый PDFДокумент()  // что если завтра понадобится Word?
            документ.Сформировать()
        }
    }

    // ✅ Хорошо: фабричный метод
    абстрактный класс Отчёт {
        абстрактный метод СоздатьДокумент()
        
        метод Сформировать() {
            документ = СоздатьДокумент()
            документ.Собрать()
            документ.Сохранить()
        }
    }

    класс PDFОтчёт : Отчёт {
        метод СоздатьДокумент() {
            возврат новый PDFДокумент()
        }
    }

    класс WordОтчёт : Отчёт {
        метод СоздатьДокумент() {
            возврат новый WordДокумент()
        }
    }
    ```

    Когда использовать: Большая иерархия объектов и неизвестно, какой будет создан в рантайме.

    Когда не использовать: у тебя один тип объектов, нет иерархии наследования. Фабрика не нужна.

2. Builder (Строитель)

    Проблема категории Г: Объект сложный, с кучей параметров и конструктор превращается в простыню 15-ти аргументов.

    Решение: объект-строитель, который собирает пошагово объект.

    ```c#
    // ❌ Плохо: конструктор-монстр
    класс Отчёт {
        конструктор(заголовок, автор, дата, списокСтраниц, цветФона, шрифт, размерШрифта, ...) {
            // 20 параметров
        }
    }

    // ✅ Хорошо: строитель
    класс Отчёт {
        поле заголовок
        поле автор
        поле дата
        поле списокСтраниц
        // ...
    }

    класс СтроительОтчёта {
        поле отчёт = новый Отчёт()
        
        метод СЗаголовком(текст) {
            отчёт.заголовок = текст
            возврат этот
        }
        метод САвтором(имя) {
            отчёт.автор = имя
            возврат этот
        }
        метод СДатой(дата) {
            отчёт.дата = дата
            возврат этот
        }
        метод Построить() {
            возврат отчёт
        }
    }

    // Использование:
    отчёт = новый СтроительОтчёта()
        .СЗаголовком("Годовой отчёт")
        .САвтором("Иванов")
        .СДатой(сегодня)
        .Построить()
    ```

    Когда использовать: Когда у объекта много параметров, и некоторые из них опциональны.

    Когда не использовать: Если у объекта 2-3 параметра — просто используй конструктор или new { ... }.

3. Singleton (Одиночка) - антипаттерн!

    Проблема: Нужно, чтобы у класса был только один экземпляр во всей программе.

    Решение: Делаешь конструктор приватным, а доступ к единственному экземпляру — через статический метод.

    Почему это антипаттерн:
    - Нарушает SRP — класс отвечает и за свою логику, и за управление своим экземпляром
    - Создаёт глобальное состояние — трудно тестировать, трудно отлаживать
    - Порождает скрытые зависимости — код, использующий синглтон, не говорит об этом явно
    - В многопоточной среде требует осторожной синхронизации

    ```c#
    // ❌ Плохо: синглтон
    класс Логгер {
        частный статический Логгер экземпляр
        
        частный Логгер() { }
        
        статический метод ПолучитьЭкземпляр() {
            если (экземпляр == null) экземпляр = новый Логгер()
            возврат экземпляр
        }
        
        метод Записать(сообщение) { ... }
    }

    // ✅ Хорошо: просто передавай зависимость через конструктор
    класс Логгер {
        метод Записать(сообщение) { ... }
    }

    класс Сервис {
        частный Логгер логгер
        
        конструктор(Логгер л) {
            логгер = л
        }
    }
    ```

    Когда использовать Singleton: Почти никогда. Если очень надо — используй DI-контейнер, который управляет временем жизни объектов (SingleInstance).

### Структурные паттерны

1. Adapter (Адаптер)

    Проблема: У тебя есть класс с одним интерфейсом, а код ожидает другой интерфейс. Как подружить несовместимое?

    Решение: Создаёшь прослойку-адаптер, которая преобразует один интерфейс в другой.

    ```c#
    // Есть старая система
    класс СтараяСистема {
        метод ОтправитьДанные(строка данные) { ... }
    }

    // Новый код ожидает такой интерфейс
    интерфейс IНовыйОтправитель {
        метод Отправить(объект данные)
    }

    // Адаптер
    класс Адаптер : IНовыйОтправитель {
        частный СтараяСистема старая
        
        конструктор(СтараяСистема с) {
            старая = с
        }
        
        метод Отправить(объект данные) {
            строка json = Сериализовать(данные)
            старая.ОтправитьДанные(json)
        }
    }
    ```

    Когда использовать: Когда ты не можешь изменить старый код, но должен его использовать в новом контексте.

2. Decorator (Декоратор)

    Проблема: Нужно добавлять функциональность объекту динамически, но наследование создаёт слишком много классов.

    Решение: Оборачиваешь объект в другой объект, который добавляет новое поведение.

    ```c#
    интерфейс IКофе {
        метод Стоимость()
        метод Описание()
    }

    класс Эспрессо : IКофе {
        метод Стоимость() { возврат 100 }
        метод Описание() { возврат "Эспрессо" }
    }

    // Декоратор
    абстрактный класс ДекораторКофе : IКофе {
        защищённый IКофе кофе
        
        конструктор(IКофе к) { кофе = к }
        
        метод Стоимость() { возврат кофе.Стоимость() }
        метод Описание() { возврат кофе.Описание() }
    }

    класс СМолоком : ДекораторКофе {
        конструктор(IКофе к) : база(к) { }
        
        метод Стоимость() { возврат база.Стоимость() + 30 }
        метод Описание() { возврат база.Описание() + ", с молоком" }
    }

    класс ССахаром : ДекораторКофе {
        конструктор(IКофе к) : база(к) { }
        
        метод Стоимость() { возврат база.Стоимость() + 10 }
        метод Описание() { возврат база.Описание() + ", с сахаром" }
    }

    // Использование:
    кофе = новый ССахаром(новый СМолоком(новый Эспрессо()))
    // Итог: Эспрессо, с молоком, с сахаром — 140 рублей
    ```

    Когда использовать: Когда нужно гибко добавлять функциональность без создания кучи подклассов.

    Альтернатива: В C# можно использовать extension methods для простых случаев.

### Поведенческие паттерны

1. Strategy (Стратегия)

    Проблема: У тебя есть алгоритм, который должен вести себя по-разному в зависимости от ситуации.

    Решение: Выносишь алгоритм в отдельный класс — стратегию, и передаёшь его в основной класс.

    ```c#
    интерфейс IСпособОплаты {
        метод Оплатить(сумма)
    }

    класс Карта : IСпособОплаты {
        метод Оплатить(сумма) { /* списать с карты */ }
    }

    класс Наличные : IСпособОплаты {
        метод Оплатить(сумма) { /* принять наличные */ }
    }

    класс Криптовалюта : IСпособОплаты {
        метод Оплатить(сумма) { /* перевести крипту */ }
    }

    класс Касса {
        частный IСпособОплаты способ
        
        конструктор(IСпособОплаты с) { способ = с }
        
        метод ПровестиПлатёж(сумма) {
            способ.Оплатить(сумма)
        }
    }

    // Использование:
    касса = новый Касса(новый Карта())
    касса.ПровестиПлатёж(1000)
    ```

    Когда использовать: Когда у тебя есть несколько вариантов поведения, которые могут меняться в рантайме.

2. Observer (Наблюдатель)

    Проблема: Когда один объект меняет состояние, другие объекты должны узнать об этом и отреагировать.

    Решение: Создаёшь механизм подписки: наблюдатели подписываются на события, а объект оповещает всех подписчиков.

    ```c#
    // Событие — это встроенный observer в C#
    класс Магазин {
        событие Действие<Товар> ТоварПоявился
        
        метод ДобавитьТовар(товар) {
            // добавить товар
            ТоварПоявился?.Вызвать(товар)  // оповестить всех
        }
    }

    класс Покупатель {
        метод Купить(товар) {
            // купить товар
        }
    }

    // Использование:
    магазин = новый Магазин()
    покупатель = новый Покупатель()

    магазин.ТоварПоявился += покупатель.Купить  // подписка
    магазин.ДобавитьТовар(новый Товар("Хлеб"))  // покупатель узнает
    ```

    Когда использовать: Когда у тебя есть связь «один-ко-многим» и объекты должны реагировать на изменения друг друга.

    В C# это уже встроено — события и делегаты делают observer тривиальным.

3. Template Method (Шаблонный метод)

    Проблема: У тебя есть алгоритм с фиксированной последовательностью шагов, но некоторые шаги могут отличаться в разных реализациях.

    Решение: Базовый класс определяет скелет алгоритма, а подклассы переопределяют отдельные шаги.

    ```c#
    абстрактный класс Напиток {
        // Шаблонный метод — фиксирует последовательность
        метод Приготовить() {
                НагретьВоду()
                Заварить()
                НалитьВЧашку()
                ДобавитьДобавки()
        }
        
        метод НагретьВоду() { /* общая реализация */ }
        метод НалитьВЧашку() { /* общая реализация */ }
        
        абстрактный метод Заварить()
        абстрактный метод ДобавитьДобавки()
    }

    класс Чай : Напиток {
        метод Заварить() { засыпатьЧай() }
        метод ДобавитьДобавки() { добавитьСахар() }
    }

    класс Кофе : Напиток {
        метод Заварить() { сваритьЗерна() }
        метод ДобавитьДобавки() { добавитьМолоко() }
    }
    ```

    Когда использовать: Когда у тебя есть алгоритм с общими и вариативными частями.

### Паттерны, которые почти не нужны в современном C#
Паттерн	- Почему не нужен:  
- Singleton -	Антипаттерн. Используй DI-контейнер
- Abstract Factory -	Заменяется DI-контейнерами и фабриками с делегатами
- Prototype -	Заменяется клонированием через MemberwiseClone или сериализацией, но редко нужно
- Visitor	Заменяется pattern matching и switch expressions в C# 8+
- Iterator -	В C# есть foreach, IEnumerable, LINQ
- Command -	Заменяется делегатами и лямбдами
- Mediator -	Полезен, но есть готовые библиотеки (MediatR)
- Flyweight -	Актуален только для экстремальной оптимизации памяти
- Interpreter -	Почти никогда не нужен — используй готовые парсеры
- Bridge -	Часто избыточен — используй композицию напрямую