<!--
============================================================
ДЛЯ AI (Чат-помощник по C#)
============================================================

Привет! Это основной файл-навигатор моего конспекта по C# и .NET.

📌 СТРУКТУРА
- Папки: Junior/ Middle/ Senior/ – по уровням сложности.
- Файлы нумеруются: 010-Тема.md, 020-Тема.md и т.д. (шаг 10).
- Между темами можно вставлять 011-... или 011a-...
- Текущий файл (этот) – оглавление и рабочая область для новых тем.

👤 ОБО МНЕ (пользователь)
- У меня расстройство аутистического спектра (РАС).
- Мне нужна чёткая структура, без лишней информации и переключений.
- Я предпочитаю получать материал целиком (не разбивая на подпункты), если не прошу иначе.
- Я собираюсь использовать данный конспект и все файлы как HandBook, не залезая каждый раз в документацию, формируй материал так, чтобы у меня не было потребности лезть в документацию в большинстве сценариев разработки

🔍 КАК ТЫ ДОЛЖЕН РАБОТАТЬ СО МНОЙ
1. Всегда проверяй мои конспекты по официальной документации Microsoft.
2. Перед тем как давать новый материал – проверь, что я уже законспектировал, укажи на ошибки и оцени прогресс.
3. Новый материал давай сразу полностью (без деления на части), если я не говорю иначе.
4. Отвечай кратко и по делу, без воды.
5. Сообщай прогресс в процентах (сколько тем закрыто, сколько осталось).

📂 КАК МЫ РАБОТАЕМ С ЭТИМ ФАЙЛОМ
- Этот файл – оглавление и одновременно «песочница».
- Когда я изучаю новую тему, я пишу конспект прямо сюда (в соответствующий раздел).
- Позже, когда мне будет удобно, я перенесу содержимое в отдельный файл в папку уровня (Junior/Middle/Senior) и обновлю ссылку.
- Поэтому в этом файле могут быть как готовые темы (со ссылками), так и черновики (без ссылок).

✅ ТЕКУЩИЙ ПРОГРЕСС
- Junior: все темы закрыты (есть файлы).
- Middle: частично закрыты (есть файлы по Дженерикам, Делегатам, Асинхронности, Сериализации). Остальное – в плане.
- Senior: только Сборщик мусора. Остальное – в плане.

Планируемые темы (будут добавлены позже) перечислены в соответствующих разделах.

📌 КОГДА ТЫ НАЧИНАЕШЬ НОВЫЙ ДИАЛОГ
Просто прочитай этот файл – и ты будешь в курсе всей структуры, моих особенностей и нашего подхода.

📂 МОЙ РЕПОЗИТОРИЙ
- **URL:** https://github.com/kamnetanker/MyITBrain
- **Директория с конспектом:** `CSharp/`
- **Структура:** `Junior/`, `Middle/`, `Senior/`, файл `Current.md` (этот файл)
- **Доступ:** публичный, ты можешь читать любые файлы в любой момент

🔍 КАК ТЫ МОЖЕШЬ ИСПОЛЬЗОВАТЬ РЕПОЗИТОРИЙ
1. Если я прошу проверить конспект — ты открываешь нужный файл и сверяешь с документацией Microsoft.
2. Если я прошу дополнить тему — ты смотришь, что уже есть, и даёшь недостающее.
3. Если я прошу привести пример из реального проекта — ты можешь поискать подходящий репозиторий на GitHub (публичный) и показать его структуру или код.
4. Если я говорю «посмотри в моём репозитории» — ты знаешь, где искать.
5. Я обновляю конспекты (включая Current.md) локально в процессе работы, но в репозиторий на GitHub изменения попадают только в конце рабочей сессии (или после завершения блока тем). Поэтому актуальная версия на GitHub может отставать от моей локальной — если нужно свериться с последними правками, уточняй у меня.

============================================================
-->

# C# и .NET — дорожная карта (оглавление)

## Junior (база – обязательный минимум)

- [010 - Типы данных](./Junior/010-ТипыДанных.md)
- [020 - Математика](./Junior/020-Математика.md)
- [030 - Управляющие конструкции](./Junior/030-УправляющиеКонструкции.md)
- [040 - Структуры данных](./Junior/040-СтруктурыДанных.md)
- [050 - Строки](./Junior/050-Строки.md)
- [060 - Исключения](./Junior/060-Исключения.md)
- [070 - Функции](./Junior/070-Функции.md)
- [080 - Классы](./Junior/080-Классы.md)
- [090 - Интерфейсы](./Junior/090-Интерфейсы.md)
- [100 - Инкапсуляция](./Junior/100-Инкапсуляция.md)
- [110 - Наследование](./Junior/110-Наследование.md)
- [120 - Полиморфизм](./Junior/120-Полиморфизм.md)
- [130 - Потоки ввода-вывода](./Junior/130-ПотокиВвода-Вывода.md)
- [140 - Дата и время](./Junior/140-ДатаВремя.md)
- [150 - Замер времени](./Junior/150-ЗамерВремени.md)

---

## Middle (профессиональные инструменты)

- [010 - Дженерики](./Middle/010-Дженерики.md)
- [020 - Делегаты и события](./Middle/020-ДелегатыИСобытия.md)
- [030 - Асинхронность и многопоточность](./Middle/030-АсинхронностьИМногопоточность.md)
- [040 - Сериализация](./Middle/040-Сериализация.md)
- [050 - Рефлексия и атрибуты](./Middle/050-РефлексияАтрибуты.md)

**Планируемые темы (будут добавлены позже):**

- SOLID
- Паттерны проектирования
- Архитектурные паттерны
- LINQ
- **Базы данных и SQL (основы)**
- **Entity Framework Core**
- ASP.NET Core Web API
- **Тестирование (xUnit, Moq)**
- **Логирование (Serilog)**
- **Кэширование (MemoryCache, Redis)**
- **Фоновые задачи (BackgroundService, Hangfire)**

---

## Senior (экспертиза и глубина)

- [010 - Сборщик мусора](./Senior/010-СборщикМусора.md)

**Планируемые темы (будут добавлены позже):**  

- Проекты (структура `.sln` / `.csproj`)
- Git и GitHub workflow
- NuGet
- Модель памяти .NET (Stack, Managed/Unmanaged Heap, барьеры)
- Продвинутая отладка и профилирование
- Безопасность в .NET (криптография, защита от атак)
- **Микросервисы (gRPC, Message Queues)**
- **Контейнеризация и оркестрация (Docker, Kubernetes — основы)**
- **Наблюдаемость (Prometheus, OpenTelemetry)**
- **Безопасность (JWT, OAuth2)**
- **Профилирование и оптимизация производительности**
- Blazor WASM
- SourceGenerators

---

## Текущие темы (ещё не распределены)

Эти темы уже есть в `Current.md` и ждут своего часа. Они будут добавлены в соответствующие разделы после того, как я создам для них файлы.


- Проекты (структура `.sln` / `.csproj`) (`Senior/040-УправлениеПроектами.md`)
- Git и GitHub workflow (`Senior/050-GitWorkflow.md`)
- NuGet (`Senior/060-NuGet.md`)
- SOLID (`Middle/070-SOLID.md`)
- Паттерны проектирования (`Middle/080-ПаттерныПроектирования.md`)
- Архитектурные паттерны (`Middle/090-АрхитектурныеПаттерны.md`)
- LINQ (`Middle/050-LINQ.md`)
- ASP.NET Core Web API (`Middle/100-ASPNETCoreWebAPI.md`)
- Blazor WASM (`Senior/070-BlazorWASM.md`)

## Планируемые практические темы

### Создание приложения с поддержкой плагинов

**Цель:** научиться проектировать приложения так, чтобы третьи лица могли расширять их функциональность без изменения основного кода (десктоп, мобилка, игры).

**Подтемы:**
- Архитектура "Хост → Контракт → Плагин"
- Создание общего контракта (интерфейсы `IPlugin`, `ICommand` и т.п.)
- Динамическая загрузка плагинов через рефлексию
- Современная изоляция: `AssemblyLoadContext` и `AssemblyDependencyResolver`
- Интеграция с Dependency Injection (DI)
- Обнаружение плагинов через кастомные атрибуты
- Жизненный цикл плагина (загрузка → инициализация → активация → выгрузка)
- Ограничения мобильных платформ (iOS AOT, Android)
- Безопасность и песочница

---

**Практический пример:** создание калькулятора с командами, где каждая команда — отдельный плагин (пример из документации Microsoft).

### Разработка игры на Godot / Redot Engine (C#)

**Цель:** освоить создание игр на Godot (или его форке Redot) с использованием C# — от настройки среды до публикации на десктоп и мобильные платформы.

**Подтемы:**
- Установка Godot (.NET-версия) и настройка редактора под C#
- Создание первого скрипта на C# (вместо GDScript)[reference:0]
- Основы Godot API из C#: Node, SceneTree, сигналы, таймеры[reference:1]
- Работа с 2D/3D-физикой и анимациями через C#
- Интеграция сторонних .NET-библиотек (NuGet) в игровой проект
- Экспорт проекта на десктоп (Windows, Linux, macOS)[reference:2]
- Экспорт на Android и iOS (AOT-компиляция, особенности)[reference:3][reference:4]
- Создание игровых систем: состояния, компоненты, инвентарь, сохранения

**Практический пример:** создание простой 2D-игры (арканоид/платформер) с нуля на C#.

**Ресурсы:** Microsoft Godot C# Essentials[reference:5], официальная документация Godot C#[reference:6].

---

### Мобильное приложение-чат для локальной сети (P2P)

**Цель:** разработать кроссплатформенное мобильное приложение для обмена сообщениями напрямую между устройствами в одной локальной сети, без центрального сервера.

**Подтемы:**
- **Обнаружение соседей (Discovery):** mDNS / Zeroconf / Bonjour[reference:7] — автоматический поиск других устройств в локальной сети.
- **Peer-to-Peer соединение:** установка прямого TCP-соединения между найденными устройствами[reference:8].
- **Альтернативный подход:** WebRTC с DataChannel для P2P-связи (обход NAT, работа через интернет)[reference:9][reference:10].
- **Обмен сообщениями:** текстовые сообщения, уведомления о статусе (онлайн/офлайн), история чата.
- **Кроссплатформенность:** .NET MAUI / Avalonia UI / Uno Platform для единого кода под Android и iOS.
- **Разрешения:** сетевые разрешения на мобильных платформах (Android, iOS)[reference:11].
- **Безопасность:** шифрование сообщений (опционально).

**Практический пример:** консольный или мобильный чат-клиент, который автоматически находит соседей в Wi-Fi сети и позволяет обмениваться сообщениями.

**Ресурсы:** Zeroconf (NuGet)[reference:12], примеры P2P-чатов на GitHub[reference:13][reference:14].

### Веб-приложение "Менеджер задач" (Todo API) на ASP.NET Core

**Цель:** создать полноценный бэкенд для управления задачами с хранением в базе данных, авторизацией, логированием и интеграцией с внешним API. Это классический проект, который показывает, как работают вместе все ключевые Middle-технологии.

**Подтемы:**
- Проектирование моделей данных (Entity Framework Core, Code First, миграции)
- RESTful API на ASP.NET Core (контроллеры, маршрутизация, модель-биндинг, валидация)
- Dependency Injection (регистрация сервисов, работа с ILogger, IConfiguration)
- Работа с БД: PostgreSQL / SQLite / SQL Server через EF Core (запросы, LINQ, отношения)
- Аутентификация и авторизация (JWT, регистрация/вход пользователей)
- Кэширование (IMemoryCache, IDistributedCache + Redis)
- Логирование (Serilog / ILogger, structured logging, запись в файл или консоль)
- Фоновые задачи (IHostedService / BackgroundService) — например, очистка старых задач
- Интеграция с внешним API (например, получение курсов валют или погоды через HttpClient)
- Тестирование: Unit-тесты (xUnit + Moq), интеграционные тесты (TestContainers)
- Документирование API (Swagger / OpenAPI)

**Практический пример:** API для управления задачами с пользователями, категориями, приоритетами, сроками выполнения и уведомлениями.

**Ресурсы:** официальные туториалы Microsoft по ASP.NET Core, EF Core, JWT, xUnit, Serilog.

---

# Текущая работа



## Принципы программирования (в дополнение к SOLID)

## SOLID

SOLID - 5 принципов проектирования программ, введённых Робертом Мартином. Цель - код должен быть легко поддерживаемым, легко расширяемым и легко понимаемым. 

Принципы SOLID это не истины выбитые на скрижалях и имеют множество позиций критики, когда слепое следование этим принципам приводило к тому, что система была неподдерживаема. Часть из этих примеров связана с неправильным пониманием SOLID, часть с тем, что SOLID применялся там, где ему не место. Об этом в подразделе "Критика".

### S - Single Responsibility (Единственная ответственность)

У класса только одна ответственная зона - его состояние, определяемое областью функционирования.

Класс, ответственный за данные пользователя не должен содержать поле под файловый поток, потому что это не его зона ответственности, как и не должен содержать методов напрямую работающих с файловым вводом-выводом.

```text
// ❌ ПЛОХО: Один класс делает всё
класс Пользователь{
    метод СохранитьНаДиск(аргумент путь); // Сохраняет на диск
    метод ЗагрузитьСДиска(аргумент путь); // Читает с диска
    метод СоздатьПользователя(аргумент данные); // Создание экземпляра пользователя
    метод ИзменитьДанные(аргумент данные); // Меняет данные пользователя
}

/*
В вышеприведённом примере, чтение и запись с диска и на диск должны быть реализованы в классе, работающем с диском, создание из данных экземпляров (парсинг, десериализация) это задача фабрики, возврат нового экземпляра из данных это тоже задача фабрики. Единственное, что должен делать класс пользователя - работать с собственными данными, на основе аргументов. 
*/

// ✅ ХОРОШО: Каждый класс отвечает за одно
класс ФайловыйЧитун{ // Единственная ответственность - работа с файлами
    метод ПрочитатьСДискаТекст(аргумент путь);
    метод ПрочитатьСДискаБинарные(аргумент путь);
}
класс ФабрикаОбъектов{ // Единственная ответственность - сериализация/парсинг/десериализация объектов
    метод СохранитьОбъект(аргумент путь, аргумент объект);
    метод ЗагрузитьОбъект(аргумент путь, ...); // Здесь может быть много реализаций, опустим нюансы
    метод СоздатьОбъект(аргумент данныеДляОбъекта);
}
класс Пользователь{ // Единственная ответственность - данные о пользователе
    метод ДелатьЧто-тоСДаннымиПользователя(аргумент Данные);
}
```

Когда нарушаешь: изменение логики сохранения заставляет перекомпилировать весь класс с генерацией и печатью → риск сломать всё сразу.

### O - Open/Closed класс открыт к расширению, закрыт к изменению.

Суть принципа в том, чтобы подходить к проектированию таким образом, чтобы базовая реализация методов не нарушала логики работы его родительских классов. Т.е. обобщение(апкаст) до родительского класса и алгоритмы, работающие с родительским классом, должны корректно работать с дочерними классами, дочерние классы должны расширять функциональность базового, но не изменять её.

Часто этот принцип, в том числе в популярном изложении, путают к закрытости для изменения функциональности того, что уже написано, однако это невозможно, программа меняется и эволюционирует и код может меняться, это нормально. Плодить сотни наследников под изменение принципа работы - нелогично, спагеттифицирует код, приводит к проблемам с пониманием логики работы.

Здесь разработчик находится в очень сложном положении, выход из которого только через опыт в проектировании и рефакторинг, когда опыт не вывез поставленную задачу.

С одной стороны изменение алгоритма базового класса может привести к нарушению функционирования дочерних и такие изменения необходимо планировать и документировать заранее, определяя план действий. Обратная ситуация, изменение функционирования наследников через механизм наследования, может сломать алгоритмы, работающие через обобщения, что тоже проблема. Когда возникают такие проблемы - вопрос к архитектуре приложения и как образуется логика наследования, возможно, решением является реализация интерфейсов, а не наследование, тем не менее каждый отдельный случай уникален и должен рассматриваться отдельно.

Пример кода приводить бессмысленно, поскольку в отрыве от реальной задачи он не даст исчерпывающего понимания, как делать надо, а как не надо, поскольку то, как надо в одной ситуации, примененное на другую может привести к плачевной ситуации и попа-больному рефакторингу.

### L - Подстанока Лискова (Liskov Substitution)

Принцип, вытекающий из соблюдения принципа Open/Closed - дочерние объекты не должны нарушать логику базового. т.е. дочерний объект должен быть полным корректным аналогом базового класса с точки зрения множества алгоритмов, применяемых к базовому классу. С точки зрения реализации, аналогом он может не являться, расширяя или меняя логику работы базового класса, но он должен оставаться корректным на множестве применяемых алгоритмов.

```c#
// ❌ ПЛОХО: Наследник ломает поведение родителя
класс Bird {
    метод Fly() { ... }
}

класс Penguin : Bird {
    метод Fly() { throw new Exception("Не умею летать") }  // ❌
}

функция MakeBirdFly(Bird b) {
    b.Fly()   // упало для пингвина
}
// ❌ ПЛОХО: спагеттификация, пложение интерфейсов
интерфейс IFlyable {
    метод Fly()
}

интерфейс ISwimmable {
    метод Swim()
}

класс Sparrow : IFlyable {
    метод Fly() { ... }
}

класс Penguin : ISwimmable {
    метод Swim() { ... }
}
// Животные в принципе слабо подходят для аналогий программного кода, поскольку биологическая иерархия не является функциональной иерархией, а в программировании доминантой является функциональная иерархия.
// ✅ ХОРОШО: Иерархия определена функциональными возможностями для оперирования над объектами
класс документ{
    поле название
    поле датаСоздания
    поле источник
    поле назначение
    виртуальный метод ДокументВТекст();
    метод ВернутьМетаданные();
}
класс счёт : документ {
    поле[] позиции
    поле основание
    поле сумма
    поле счётДляОплаты
    переопределённый метод ДокументВТекст();
    переопределённый метод ВернутьМетаданные();
    метод ФормироватьЧек();
}
класс служебнаяЗаписка : документ{
    переопределённый метод ДокументВТекст();
}
класс накладная : документ {
    поле[] позиции
    поле доставленоДатаВремя
    поле приянтоДатаВремя
    поле курьер
    поле ответственный
    переопределённый метод ДокументВТекст();
    переопределённый метод ВернутьМетаданные();
}
/*
Здесь все документы имеют название (для счёта это его номер, для накладной это номер, для служебной записки это заголовок определяющий суть служебной записки), каждый документ имеет дату, каждый имеет источник (кто подал этот документ или создал или отправил), каждый имеет назначение (кому адресовано (компания или фио директора или иного ответственного лица)). 
Каждый можно сериализовать в некоторый текст и из каждого можно получить перечень метаданных. Общие метаданные по принципу Open/Close и Liskov Substitution должны соответствовать базовому классу. (В качестве возвращаемых метаданных могут выступать словари) и алгоритмы, работающие с документами в целом (составление отчётов о документообороте, например) должны работать со всеми документами.
Реализации конкретных документов могут отличаться, но они не должны нарушать принцип ненарушения работы базового класса и быть корректны для алгоритмов на множестве алгоритмов для документов.
Дополнительная функциональность, не определяемая документами в целом, может быть добавлена через реализацию интерфейсов, не затрагивая функциональность основного класса.
*/
```

### I - Interface Segregation (Разделение интерфейсов)

Принцип говорит: интерфейс отвечает за единственное множество функциональности, определённое на функциональном классе. Что это значит? Интерфейс должен предписывать классу реализовать только ту функциональность, которая определяется глаголом в его названии и не должен обязывать реализовать то, что не требуется реализующим его классам.

```c#
// ❌ ПЛОХО: Огромный интерфейс на всё
интерфейс IWorker {
    метод Work()
    метод Eat()
    метод Sleep()
    метод TakeBreak()
}

класс Robot : IWorker {
    метод Work()   { ... }
    метод Eat()    { throw ... }   // не нужен
    метод Sleep()  { throw ... }   // не нужен
    метод TakeBreak() { ... }      // не нужен, но надо реализовать
}
// ✅ ХОРОШО: Разбили на мелкие интерфейсы
интерфейс IWorkable   { метод Work() }
интерфейс IEatable    { метод Eat() }
интерфейс ISleepable  { метод Sleep() }

класс Human : IWorkable, IEatable, ISleepable {
    метод Work()  { ... }
    метод Eat()   { ... }
    метод Sleep() { ... }
}

класс Robot : IWorkable {
    метод Work() { ... }   // только то, что нужно
}
```

### D — Dependency Inversion (Инверсия зависимостей)

Инверсия зависимостей может сбить с толку, при прямолинейной трактовке. Смысл в том, чтобы функциональность, которая имеет множество реализаций зависела не от конкретной реализации, а от интерфейса взаимодействия (абстракции) над этой реализацией. 

```c#
// ❌ ПЛОХО: Высокий уровень зависит от низкого
класс EmailSender {
    метод Send(message) { ... }
}

класс NotificationService {
    private EmailSender sender = new EmailSender()   // жёсткая привязка

    метод Notify(user, text) {
        sender.Send(text)
    }
}
// Чтобы заменить EmailSender на SmsSender — надо менять NotificationService
// ✅ ХОРОШО: Зависим от интерфейса, а не от конкретного класса
интерфейс IMessageSender {
    метод Send(message)
}

класс EmailSender : IMessageSender {
    метод Send(message) { ... }
}

класс SmsSender : IMessageSender {
    метод Send(message) { ... }
}

класс NotificationService {
    private IMessageSender sender

    конструктор NotificationService(IMessageSender s) {
        sender = s                         // DI — внедрение зависимости
    }

    метод Notify(user, text) {
        sender.Send(text)
    }
}
```

Когда нарушаешь: чтобы поменять способ отправки, приходится переписывать NotificationService → тестировать сложно, изменения дорогие

### KISS — Keep It Simple, Stupid
Простота — залог успеха. Большинство систем работают лучше, если они простые, а не сложные.

### DRY — Don't Repeat Yourself
Не повторяйся. Дублирование кода — источник ошибок и усложнения поддержки.

### YAGNI — You Aren't Gonna Need It
Не добавляй функциональность, пока она не стала действительно необходимой. Это экономит время и силы.

### Separation of Concerns (SoC)
Разделяй ответственность. Каждая часть программы должна решать одну задачу.

### Command Query Separation (CQS)
Метод либо изменяет состояние, либо возвращает данные, но не делает и то, и другое одновременно.

### Law of Demeter (LoD)
Объект не должен знать о внутреннем устройстве других объектов. Минимизируй связанность.

### Composition Over Inheritance
Предпочитай композицию наследованию. Это гибче и проще для поддержки.

## Критика ООП и почему процедурный/функциональный подход часто лучше

### Проблема: ООП как инструмент решения бизнес-задач
Бизнесу нужен работающий продукт здесь и сейчас, а не архитектурный шедевр на вырост. ООП заставляет думать о будущем, а не о текущей задаче, что замедляет разработку.

### Процедурный/функциональный подход: "здесь и сейчас"
- Прямой поток данных: вход → преобразование → выход.
- Минимум косвенности, нет глубоких иерархий.
- Фокус на задаче, а не на структуре.

### Почему ООП не стыкуется с реальностью бизнеса?
Бизнес меняется быстро. ООП с жёсткими иерархиями плохо адаптируется, рефакторинг дорогой и долгий. Процедурный код меняется локально и дёшево.

### Итог
В большинстве проектов процедурный/функциональный подход быстрее, проще и дешевле. ООП имеет смысл только в больших, долгоживущих системах с огромной командой.